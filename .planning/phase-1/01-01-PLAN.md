---
phase: 1
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [server/prisma/schema.prisma, server/src/middleware/authenticate.ts, server/src/middleware/authorize.ts]
autonomous: true
requirements: ["1.1", "1.3"]
must_haves:
  truths:
    - "Prisma 6 is correctly initialized with the security-enhanced User model"
    - "JWT tokens are invalidated if issued before the user's updatedAt timestamp"
  artifacts:
    - path: "server/prisma/schema.prisma"
      provides: "Prisma 6 schema with User and AuditLog models"
    - path: "server/src/middleware/authenticate.ts"
      provides: "Authentication with stateless session invalidation"
---

<objective>
Setup the Prisma 6 database schema and implement the authentication and authorization middleware with stateless session invalidation.
</objective>

<tasks>
<task type="auto">
  <name>Setup Prisma 6 Schema</name>
  <files>server/prisma/schema.prisma</files>
  <action>
    - Ensure Prisma version 6 is in package.json.
    - Update schema.prisma with Role enum (TEACHER, REGISTRAR, SYSTEM_ADMIN) and User model (isActive, mustChangePassword, updatedAt).
    - Run 'npx prisma generate' and 'npx prisma migrate dev'.
  </action>
  <verify>
    <automated>npx prisma -v && npx prisma migrate status</automated>
  </verify>
  <done>Database schema initialized and Prisma client generated.</done>
</task>

<task type="auto">
  <name>Implement Auth Middleware with Stateless Invalidation</name>
  <files>server/src/middleware/authenticate.ts, server/src/middleware/authorize.ts</files>
  <action>
    - In 'authenticate', verify JWT and compare 'iat' with 'user.updatedAt'.
    - If user.updatedAt.getTime() / 1000 > decoded.iat, return 401.
    - Implement 'authorize' middleware to check 'req.user.role' against allowed roles.
  </action>
  <verify>
    <automated>npm test server/src/middleware/authenticate.test.ts</automated>
  </verify>
  <done>Middleware correctly handles authentication, role-checking, and stateless invalidation.</done>
</task>
</tasks>
