---
phase: 1
plan: 01-03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [server/src/services/branding.ts, server/src/routes/settings.routes.ts, client/src/stores/brandingStore.ts]
autonomous: true
requirements: ["1.4"]
must_haves:
  truths:
    - "School accent color is extracted from the uploaded logo and applied dynamically"
    - "Main UI layers remain unchanged, while accent colors are skin-configurable"
  artifacts:
    - path: "server/src/services/branding.ts"
      provides: "Color extraction logic using sharp and color-thief-node"
    - path: "client/src/stores/brandingStore.ts"
      provides: "State management for dynamic CSS variable injection"
---

<objective>
Implement dynamic branding via server-side color extraction and frontend CSS variable injection using Tailwind v4.
</objective>

<tasks>
<task type="auto">
  <name>Implement Server-Side Color Extraction</name>
  <files>server/src/services/branding.ts, server/src/routes/settings.routes.ts</files>
  <action>
    - Use 'sharp' to normalize uploaded logos and 'color-thief-node' to extract the dominant color.
    - Store the extracted HSL color in 'SchoolSettings.colorScheme'.
    - Create 'GET /settings/public' to return the brand settings.
  </action>
  <verify>
    <automated>npm test server/src/services/branding.test.ts</automated>
  </verify>
  <done>Server correctly extracts and stores brand colors.</done>
</task>

<task type="auto">
  <name>Implement Frontend Dynamic Skinning</name>
  <files>client/src/stores/brandingStore.ts</files>
  <action>
    - Create a Zustand store to fetch brand settings and inject '--accent' and '--accent-foreground' into ':root'.
    - Ensure Tailwind v4 uses these variables for primary/ring colors.
  </action>
  <verify>
    <automated>npm test client/src/stores/brandingStore.test.ts</automated>
  </verify>
  <done>UI dynamically themed based on school-specific brand colors.</done>
</task>
</tasks>
