---
phase: 1
plan: 01-04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [client/src/layouts/AppLayout.tsx, client/src/components/sidebar/SidebarContent.tsx]
autonomous: true
requirements: ["1.5"]
must_haves:
  truths:
    - "AppLayout renders a responsive sidebar that adapts to user roles"
    - "Teachers only see sections relevant to them, while Admins see the full system menu"
  artifacts:
    - path: "client/src/layouts/AppLayout.tsx"
      provides: "Responsive dashboard layout container"
    - path: "client/src/components/sidebar/SidebarContent.tsx"
      provides: "Conditional navigation menu based on RBAC"
---

<objective>
Build the responsive AppLayout and implement the conditional sidebar content based on user roles.
</objective>

<tasks>
<task type="auto">
  <name>Develop AppLayout and Sidebar Base</name>
  <files>client/src/layouts/AppLayout.tsx</files>
  <action>
    - Build a collapsible sidebar using shadcn/ui primitives.
    - Ensure layout is responsive (mobile drawer vs. desktop fixed sidebar).
    - Integrate the 'brandingStore' to apply the school's logo and accent color in the header.
  </action>
  <verify>
    <automated>npm test client/src/layouts/AppLayout.test.tsx</automated>
  </verify>
  <done>Responsive dashboard layout shell completed.</done>
</task>

<task type="auto">
  <name>Implement Conditional Sidebar Navigation</name>
  <files>client/src/components/sidebar/SidebarContent.tsx</files>
  <action>
    - Map roadmap modules to sidebar navigation items.
    - Use 'useAuthStore' to render items conditionally based on 'role' (SYSTEM_ADMIN, REGISTRAR, TEACHER).
  </action>
  <verify>
    <automated>npm test client/src/components/sidebar/SidebarContent.test.tsx</automated>
  </verify>
  <done>Navigation menu correctly reflects role-based access control.</done>
</task>
</tasks>
