---
phase: 1
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [server/src/controllers/authController.ts, server/src/routes/auth.routes.ts, client/src/router/index.tsx]
autonomous: true
requirements: ["1.2"]
must_haves:
  truths:
    - "Direct access to the /login route is blocked without a specific navigation state"
    - "Backend login is protected by a single-use, 5-minute pre-flight token"
  artifacts:
    - path: "server/src/routes/auth.routes.ts"
      provides: "Two-layer login guard endpoints"
    - path: "client/src/router/index.tsx"
      provides: "Frontend navigation state guard"
---

<objective>
Implement the Two-Layer Login Guard on both the backend (pre-flight tokens) and frontend (navigation state check).
</objective>

<tasks>
<task type="auto">
  <name>Implement Backend Pre-flight Token (Layer 2)</name>
  <files>server/src/controllers/authController.ts, server/src/routes/auth.routes.ts</files>
  <action>
    - Create 'GET /auth/login-token' to issue a single-use SHA-256 hashed token with 5-minute expiry.
    - Update 'POST /auth/login' to require and validate this token before checking credentials.
  </action>
  <verify>
    <automated>npm test server/src/controllers/authController.test.ts</automated>
  </verify>
  <done>Backend login protected by pre-flight token layer.</done>
</task>

<task type="auto">
  <name>Implement Frontend Navigation Guard (Layer 1)</name>
  <files>client/src/router/index.tsx</files>
  <action>
    - Add a loader to the '/login' route to check for 'window.history.state.usr.loginAccess'.
    - Redirect to '/' if the flag is missing.
  </action>
  <verify>
    <automated>npm test client/src/router/index.test.tsx</automated>
  </verify>
  <done>Frontend login route secured against direct URL access.</done>
</task>
</tasks>
