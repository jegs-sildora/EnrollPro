Subject: Companion SSO — need the full EP→AIMS flow + feasibility of AIMS→EP

Hi! We've implemented the AIMS side of the companion-SSO contract per AIMS-SSO-Integration.md and the implementation guide — our callback (/auth/sso/callback) receives the code, we exchange it at POST /api/auth/companion-sso/aims/exchange with the Bearer secret, validate the payload, and map the identity to an AIMS account. The exchange endpoint and secret are working from our side (verified: invalid code → 400 "Authorization code is invalid").

To finish hardening this and to plan the reverse direction, could you provide:

Initiation flow (EP → AIMS): What exactly happens when a user clicks "Open AIMS" inside EnrollPro? Is there an initiate endpoint (e.g. GET /api/auth/companion-sso/AIMS/initiate)? What parameters does it accept (redirect_uri?) and where are redirect URIs registered/whitelisted on your side?
Code semantics: TTL, single-use behavior, replay/expiry error codes, and the exact HTTP status codes returned by the exchange endpoint for: invalid code, expired, replayed, wrong-system, inactive account, inactive school year.
Canonical exchange response schema: We've seen two shapes in our docs — one with schoolYear.isActive and identity.isActive, another with activeSchoolYear: { id, yearLabel } and no active flags. Which is authoritative? Please give the full request/response JSON schema (field names, types, casing) including error envelopes.
Reverse direction (AIMS → EP): Does your companion-SSO design support EnrollPro as the consumer? i.e. if AIMS issues a code and redirects the user to an EnrollPro callback, does EP have a matching exchange endpoint (e.g. POST /auth/companion-sso/aims/exchange on the AIMS side, or an EP callback URL that accepts ?code=)? If not, is there another mechanism — EP consuming an AIMS-issued JWT, or a shared cookie/session domain?
Roles & subject format: The canonical role list for EP→AIMS (we allow SYSTEM_ADMIN, HEAD_REGISTRAR, TEACHER, CLASS_ADVISER) and the expected subject format (ENROLLPRO_USER:1? userId always present?) — and what subject/role format EP would expect from AIMS in the reverse flow.
Happy to share our callback implementation or test against your dev instance. Thanks!

Tip: item 4 is the key one — the contract only documents EP as the identity provider, so the AIMS→EP direction likely needs new work on EP's side (or an alternative like shared JWT verification). If they say it's not supported yet, the pragmatic fallback is linking accounts (bind EP user id to AIMS user id, already in place via enrollproId) and exchanging a signed AIMS token at an EP endpoint — ask if that's on their roadmap.